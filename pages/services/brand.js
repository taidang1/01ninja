import styles from '../../styles/brand.module.css';

const brand = () => {
    return (
        <div className={styles.brand_section}>
            <h1>Brand</h1>
        </div>
    );
}

export default brand;